# Voll-Review 02.09.2026 — konsolidierte Befunde

5 parallele Read-only-Reviewer (Mailbox/MCP-Kern, API/Async, Watcher/Infra,
Frontend, Tests/Doku). ✔ = vom Reviewer am laufenden Code reproduziert.
Quelle in Klammern. Status-Spalte wird beim Fixen gepflegt.

**Zählung: 11 Code-H · 4 Doku-H · 2 Struktur-H (Testlücken) · ~30 M · ~25 N.**

**ABSCHLUSS 02.09.2026: ALLE Befunde gefixt — Wellen A–G komplett**
(A/B: P0 · C: P1 · D/E: P2/P3 Code · F: Doku · G: Testlücken; Commits
39970ad, c6858cd, bcad4f8, db47d13 + Abschluss-Commit; 21 Testmodule +
test_woerter.mjs + Browser-Prüfstand, npm build grün).
Die Rechte-Schnittmenge (Rollen), Kanal-Identität, Path-Traversal über Namen,
Tool-Allowlists und die Planer-Serialisierung wurden gezielt attackiert und
haben gehalten (dokumentierte Widerlegungen bei den Reviewern).

## P0 — Sicherheit / Datenverlust (sofort)

| # | ✔ | Befund | Stelle |
|---|---|--------|--------|
| P0-1 ✅ GEFIXT | ✔ | Task-/Envelope-IDs roh in Pfade gejoint — gebundener Kanal bricht per `complete_task(task_id="../../opfer/inbox/task-1")` aus der eigenen Mailbox aus: Exfiltration der Instruction, gefälschte Response, Doppelausführung. ID-Regex in die Mailbox-PRIMITIVEN (claim_task, task_offen, mark_read, _write_response, beantworte_frage, schliesse_frage) | mailbox.py:664ff (Kern) |
| P0-2 ✅ GEFIXT | ✔ | Datei-Transport claimt ohne `claimed_at` — Task mit >3 h Inbox-Liegezeit (bei `nicht_vor` der Normalfall!) gilt sofort als verwaist → pflege requeued → **Doppelausführung**. Nach `os.replace` Envelope mit claimed_at/status neu schreiben | agent_watcher.py:941 (Watcher) |
| P0-3 ✅ GEFIXT | ✔ | Outbox-Schreibfehler → `finally` räumt Task trotzdem ab = Ergebnis **spurlos weg** + Watcher stirbt am unbehandelten Fehler. Nur bei Erfolg abräumen, sonst in .processing lassen; Schleifenkörper kapseln | agent_watcher.py:1009-1022 (Watcher) |
| P0-4 ✅ GEFIXT | ✔ | RollenDialog: Text-Zuweisung an `textRef` vor DOM-Mount → erstes Anklicken zeigt leeres Feld, „Speichern" **überschreibt Rollen-Datei mit Leertext**. State+defaultValue/key-Muster wie ZeitplaeneDialog; leeren Text sperren | RollenDialog.jsx:50-72 (FE) |
| P0-5 ✅ GEFIXT | ✔ | `_stempel` verschluckt ZeitplanFehler bei kaputtem Nachbar-Plan → Stempel wird nie geschrieben → gültiger Plan feuert **Task-Salve** (jeden Tick). Stempeln ohne Nachbar-Validierung; Fehler loggen | zeitplaene.py:202-210 (API) |
| P0-6 ✅ GEFIXT | ✔ | PUT übernimmt Client-`letzter_lauf` bedingungslos → offener Dialog + Planer-Feuer + Umschalten = **Doppelfeuer**. letzter_lauf serverseitig behandeln (Client-Wert ignorieren) | zeitplaene.py:110-131 + main.py PUT (API) |
| P0-7 ✅ GEFIXT | ✔ | Rollen-Frontmatter ohne schließendes `---` → Rechte **still ignoriert**, YAML-Kopf landet im Prompt, Lauf bekommt volle Agenten-Rechte. `---`-Beginn ohne Abschluss = RollenFehler | rollen.py:35,51-62 (Kern) |
| P0-8 ✅ GEFIXT | ✔ | entrypoint `cp` ohne `-r`: ein Unterverzeichnis in config/ (legt make_agent_token.sh selbst an: tokens/) → **Container-Neustart-Schleife** | entrypoint.sh:56-63 (Watcher) |

## P1 — hoch (zeitnah)

| # | ✔ | Befund | Stelle |
|---|---|--------|--------|
| P1-1 ✅ GEFIXT | | 5 Endpunkte machen flock-blockierendes Mailbox-I/O im Event-Loop (read-all nimmt Lock je Envelope!) → Terminals/SSE/API stehen, solange MCP-Prozess/Pflege den Lock hält. `asyncio.to_thread` (Muster liegt vor) | main.py:412,428,483,513,541 (API) |
| P1-2 ✅ GEFIXT | ✔ | Kein SIGTERM/SIGHUP-Handler im Watcher → kill/Reboot hinterlässt **verwaiste claude-Prozessgruppe** (arbeitet weiter, ändert Dateien). signal-Handler → HART+abbrechen+exit(143), atexit | agent_watcher.py (Watcher) |
| P1-3 ✅ GEFIXT | | Windows: `claude.cmd`-Shim → cmd.exe parst Argumentzeile erneut; `&`/`|` im Task-Text = **Kommando-Injection** an der Rechte-Härtung vorbei (PMNB029!). Prompt via stdin/Tempdatei statt argv (löst auch ARG_MAX-M) | agent_watcher.py:427-441 (Watcher) |
| P1-4 ✅ GEFIXT | ✔ | ZeitplaeneDialog: `tage: null` aus handgepflegtem YAML → TypeError → **weiße Seite** (kein ErrorBoundary). Defensiv normalisieren + serverseitig in lade_plaene + ErrorBoundary je Panel | ZeitplaeneDialog.jsx:127ff (FE) |
| P1-5 ✅ GEFIXT | ✔✔ | `nicht_vor` naiv gespeichert: Datei-Transport-Agent (UTC) startet 22-Uhr-Task um 00:00; Browser-Badge = Browser-Zeit. Serverseitig tz-behaftet einfrieren (astimezone beim send_task); `Z`-Suffix normalisieren (Py≤3.10 wirft → Task läuft sofort). Von 2 Reviewern unabhängig gefunden | mailbox/send_task/watcher (Kern+Watcher) |
| P1-6 ✅ GEFIXT | ✔ | DST-Falle Planer: Pläne 02:00–02:59 laufen bei Rückstellung **doppelt**, bei Vorstellung **gar nicht** (fixe Offset-tzinfo statt ZoneInfo). ZoneInfo + Kalendertag-Vergleich | zeitplaene.py:146-186 (API) |
| P1-7 ✅ GEFIXT | ✔ | Geister-Mailboxen: `answer(reply_to=…)`, `read_responses(worker=…)` (Lesetool legt Verzeichnis an!), `sender` auf freiem Kanal ungeprüft → erfundene Namen zementieren sich in list_agents | mcp_server.py:255,361,541,563 (Kern) |
| P1-8 ✅ GEFIXT | ✔ | Umlaut im Admin-Passwort → `compare_digest` wirft TypeError → Login antwortet **500, dauerhaft**; gleicher Pfad mcp_token (client-gesteuert). Bytes vergleichen | auth.py:55, mcp_token.py:96 (Kern) |
| P1-9 ✅ GEFIXT | | `manager.stop()` fehlt: Shutdown-„Hart-Stopp" wird vom weiterlaufenden Reconcile ≤15 s später **rückgängig gemacht** (startet frische Watcher beim Herunterfahren) | auto_watcher/main Shutdown (API) |
| P1-10 ✅ GEFIXT | ✔ | Datei-Watcher nimmt den Mailbox-flock nie → Server-Rückschreiber (link/resolve_question) kann abgeräumten Task **wiederbeleben** → Zombie-Requeue → Doppellauf. flock im Watcher (stdlib) oder Existenz-Check unterm Lock | mailbox↔agent_watcher (Kern) |
| P1-11 ✅ GEFIXT | | Doku-H-Paket: llm-providers.yaml-Phantom; REFERENZ Roadmap+Status widersprechen dem eigenen Dokument; START.md behauptet „known_hosts deaktiviert" (TOFU ist aktiv!); .env.example INTEGRATION_TIMEOUT „600" vs. real 60 | Doku (Tests/Doku) |

## P2 — mittel

- ✅ GEFIXT ✔ complete_task-Fehlerdict gilt im Watcher als Erfolg → Ergebnis weg bei ✕/requeue (agent_watcher:767,858)
- ✅ GEFIXT ✔ Rohe `print`s zwischen Claim und Lauf → BrokenPipe killt Watcher, Task hängt 3 h (agent_watcher:800ff)
- ✅ GEFIXT ✔ stream-json `assistant`-Zweig ohne isinstance-Guard → drei kaputte Events = Automatik „gesperrt" (agent_watcher:497)
- ✅ GEFIXT `wecker.cancel()` vor `proc.wait()` ohne Timeout → Watcher kann unbegrenzt hängen (agent_watcher:544)
- ✅ GEFIXT Popen ohne encoding="utf-8" → Windows-Mojibake/UnicodeDecodeError-Abbruch (agent_watcher:430)
- ✅ GEFIXT agent_tasks liest komplette Outbox (30 Tage, ungedeckelt) alle 8 s je Agent synchron im Loop; ebenso tick→ist_ueber_schwelle und chat_store.load/save → to_thread + mtime-Vorfilter/Cap (main/verbrauch/zeitplaene)
- ✅ GEFIXT ✔ link_question heftet Fragen auch an GEPARKTE Tasks → fremde Antwort im Prompt, Task hängt an fremder Frage (mailbox:518)
- ✅ GEFIXT remote_download/raw: Fehler nach Response-Start → HTTP 200 mit leerem Body statt 502 (main:866)
- ✅ GEFIXT make_agent_token.sh: Token für uid 10001 unlesbar → dokumentierter Weg funktioniert nie (fail-closed) (scripts)
- ✅ GEFIXT Schnittmenge: engere Rollen-Regel (`Bash(git commit:*)` ⊄ exakt) läuft leer ohne Logzeile → opakes Scheitern (agent_watcher:386)
- ✅ GEFIXT Modal: Escape schließt gestapelte Dialoge doppelt (Modal.jsx:12)
- ✅ GEFIXT GBoard-Regel in neuen Dialogen verletzt (RollenDialog:117, ZeitplaeneDialog:145; Altbestand WorkspaceViews:63)
- ✅ GEFIXT Entwurf-Reset ohne Remount: erneutes Antippen des offenen Plans → Anzeige ≠ State, „Speichern" schreibt Altes (ZeitplaeneDialog:179)
- ✅ GEFIXT Chat-Fehlerpfad legt `text` statt `volltext` zurück → Anhang-Pfade verloren (Chat.jsx:242)
- ✅ GEFIXT sw.js: Share-Upload-Fehler verschluckt; kein pushsubscriptionchange-Handler → stiller Push-Totalausfall; „Öffnen" fest deutsch; 3 Actions bei maxActions 2
- ✅ GEFIXT i18n-Rest: api.js 2 Fehlertexte ohne t(); 1 fehlender Schlüssel (QuestionsBanner:207)
- ✅ GEFIXT localStorage ungeschützt in App/Chat/Terminal (WebView „Cookies blockieren" → weiße Seite) + fehlendes ErrorBoundary
- ✅ GEFIXT events: Neustart-Baseline verliert Störungs-Eingänge → kein Push für needs_confirm (events:257)
- ✅ GEFIXT push: threading.Lock aus dem Loop gerufen (push:106)
- ✅ GEFIXT verbrauch: responded_at vom Agenten-PC → Tages-Bucket in fremder TZ verschoben (verbrauch:75)
- ✅ GEFIXT Testlücken-Paket: auto_watcher.py (422 Z.) und llm.py-Ollama-Pfad KOMPLETT ungetestet (Struktur-H); E2E rolle/nicht_vor über MCP-Layer; tick(); complete_task(verbrauch)→aggregiere; integrations.py; sprache.js-Reload-Logik; Dialoge im Prüfstand (fetchDoppel um /api/rollen+/api/zeitplaene ergänzen)
- ✅ GEFIXT .env.example: PLANER_KULANZ/-INTERVALL, MAILBOX_INBOX_TAGE, AUTO_KILL_GRACE, MCP_INTEGRATION_PARALLEL u. a. fehlen

## P3 — niedrig (N-Sammlung)

**Stand 02.09. (nach Welle F/G): die GESAMTE P3-Sammlung ist erledigt** —
einschließlich SettingsIn⊆ALLOWED_KEYS (jetzt Test in test_review_fixes)
und der REFERENZ-Doku-Punkte (files-/API-Tabelle, Konfig-Baum, send_task).

mcp_token-Fehlversuchs-Dict wächst unbegrenzt · _lock_for-Aufräumfenster ·
_read_jsons fängt UnicodeDecodeError nicht (eine kaputte Datei = Panel tot
für alle) · _mcp_weiterleiten Client-Leck bei InvalidURL · tick: _poste
außerhalb des try · Ollama call_-IDs kollidieren rundenübergreifend
(Provider-Wechsel-Falle) · save_upload: Zielpfad ohne GESPERRT-Prüfung
(chat.db überschreibbar) · _CONN_NAME_RE fehlt in pubkey/delete ·
claim_tasks stempelt außerhalb des Locks · schliesse_frage 500 nach Erfolg ·
MCP-Watcher pollt geplante Tasks jede Runde (Log-Rauschen) · verbrauch
Infinity → OverflowError · deliver_response legt Geister-Mailboxen an ·
Komma-Split zerlegt Tool-Regeln mit Komma · kaputte Task-JSON verschwindet
lautlos · McpClient kennt keinen Token-Kanal · pids_limit/server_tokens ·
instanz_lock-Meldung widerspricht Kommentar · Terminal-Kopiermodus scrollt
beim Kopieren ans Ende · viewport-Blur am Desktop · doppelte ext-Fenster-
Namen · ?tab=ext:… fällt still auf Chat · Workspace setItem im Updater ·
sid ohne encodeURIComponent · AgentsPanel selected verschwundener Agent ·
index.html lang fest „de" · gesperrt-Zustand unsichtbar + toter
Wörterbucheintrag · „Löschen fehlgeschlagen"-Duplikat mit zwei
Übersetzungen · SettingsIn⊆ALLOWED_KEYS als Test statt Kommentar ·
role/mode in agents.yaml tote Felder · files.py-Tabelle unvollständig
